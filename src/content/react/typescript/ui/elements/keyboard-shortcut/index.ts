/**
 * @name Keyboard Shortcut
 * @description Invisible helper that turns the nearest actionable wrapper it is nested inside into a keyboard shortcut target, and renders a visual key hint.
 */
/** KeyboardShortcut props type. */
export * from './keyboard-shortcut.type';

/** KeyboardShortcut component. */
export * from './KeyboardShortcut';
