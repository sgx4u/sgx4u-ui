/**
 * @name Carousel
 * @description Slideshow container that reveals one slide at a time, with optional looping and autoplay.
 */
/** Carousel props type. */
export * from './carousel.type';

/** Carousel component. */
export * from './Carousel';
