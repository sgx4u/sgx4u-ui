/**
 * @name Slot
 * @description Composition utility that lets components render their children as the actual DOM element while still receiving styling and behavior.
 */
/** Slot component. */
export * from './Slot';
